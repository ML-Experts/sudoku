# UC-11 — Machine Learning

## Udział warstwy
- Brak udziału serwisu `ML` w `UC-11`.

## Endpointy wewnętrzne `BE -> ML`
- Brak.

## Kontrakty
- Brak nowych kontraktów `BE -> ML` w ramach `UC-11`.

## Modele API / DTO
- Brak nowych modeli `ApiEntry`, `ApiResponse` i `Dto`.

## Zasady działania
- `UC-11` obejmuje wyłącznie pobranie przez `FE` z `BE` listy możliwych datasetów do dalszego przygotowania.
- Skan katalogu `data/raw`, w tym przegląd podfolderów `boards` i `digits`, wykrycie kandydatów logicznych i podstawowa walidacja kandydatów należą do `BE`.
- Dla `board` kandydatem widocznym w `UC-11` jest katalog bezpośrednio pod `data/raw/boards`; rekurencyjne przeglądanie jego zawartości i odczyt par `.jpg` + `.dat` zaczynają się dopiero w `UC-12`.
- `ML` nie bierze udziału w listowaniu datasetów i nie jest wywoływany w tym use case'u.
- Właściwe przygotowanie danych i preprocessing zaczynają się dopiero w `UC-12`.
